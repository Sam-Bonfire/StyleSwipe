"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.HexagonalArchitectureDependencyRuleEnforcer = void 0;
class HexagonalArchitectureDependencyRuleEnforcer {
    constructor() {
        this.layers = ["application", "domain", "infrastructure"];
        this.dependenciesWildcard = {
            application: ["application", "domain"],
            domain: ["domain"],
            infrastructure: ["application", "domain", "infrastructure"],
        };
    }
    enforce(context, node) {
        const nodeBody = node["body"];
        if (nodeBody !== undefined) {
            nodeBody
                .filter((value) => value.type === "ImportDeclaration")
                .forEach((value) => {
                this.ensureImportIsValid(value.source.value, context, value);
            });
        }
    }
    ensureImportIsValid(importText, context, node) {
        const currentLayer = this.extractCurrentLayer(context.getFilename());
        const forbiddenImports = this.layers.filter((layer) => !this.dependenciesWildcard[currentLayer].includes(layer));
        forbiddenImports.forEach((forbiddenImport) => {
            if (importText.includes(forbiddenImport)) {
                context.report({
                    node,
                    messageId: "import-not-follow-hexagonal",
                    data: {
                        source: currentLayer,
                        target: forbiddenImport,
                    },
                });
            }
        });
    }
    extractCurrentLayer(filename) {
        const regex = /application|domain|infrastructure/g;
        const found = filename.match(regex);
        return found[0];
    }
}
exports.HexagonalArchitectureDependencyRuleEnforcer = HexagonalArchitectureDependencyRuleEnforcer;
