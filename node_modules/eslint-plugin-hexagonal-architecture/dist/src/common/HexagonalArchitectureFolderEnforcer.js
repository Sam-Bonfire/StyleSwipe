"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.HexagonalArchitectureFolderEnforcer = void 0;
class HexagonalArchitectureFolderEnforcer {
    enforce(context, node) {
        const filename = context.getFilename();
        if (!this.hasCorrectFolderStructure(filename)) {
            context.report({ node, messageId: "folder-not-follow-hexagonal" });
        }
    }
    hasCorrectFolderStructure(filename) {
        return /application|domain|infrastructure/.test(filename);
    }
}
exports.HexagonalArchitectureFolderEnforcer = HexagonalArchitectureFolderEnforcer;
