"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
const enforce_1 = __importDefault(require("./rules/enforce"));
const config = {
    rules: {
        enforce: enforce_1.default,
    },
};
module.exports = config;
