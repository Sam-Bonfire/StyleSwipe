# Infrastructure (Convex)

## Overview
This package defines the backend infrastructure and database schema using **Convex**.

## 🏗️ Structure
*   `convex/`: Database schema, query, and mutation functions.
*   `auth/`: Authentication logic configuration.

## 🚀 Deployment
Convex handles deployment automatically via `npx convex deploy` (or via CI/CD pipelines).
